# URL Shortener

Production-grade URL shortener built with Node.js, TypeScript, Fastify, Postgres, Prisma, and Redis.

## Requirements

- Node.js 20+

## Scripts

- `npm run typecheck`
- `npm run lint`
- `npm run format`
- `npm test`
